@php
 $d=$subscriptionDashboard;
 $args=['tenantId'=>$tenant->id,'program_id'=>$program->id];
 $rewardUrl=route('reseller.commission',$args);$activityUrl=route('reseller.activity',$args);$messageUrl=route('reseller.messages',$args);$detailsUrl=route('reseller.programs.show',[$tenant->id,$program->id]);
 $money=fn($minor)=>$d['currency'].' '.number_format($minor/100,2);
 $validOffers=$offers->filter(fn($offer)=>$offer->currentVersion && $offer->currentVersion->status==='published' && $offer->currentVersion->tenant_id===$tenant->id && $offer->currentVersion->program_id===$program->id);
 $chartValues=array_values($d['daily']);$chartDates=array_keys($d['daily']);
 $lo=min(0,min($chartValues));$hi=max(1,max($chartValues));$range=$hi-$lo;
 $x=fn($i)=>60+$i/max(1,count($chartValues)-1)*720;$y=fn($value)=>220-($value-$lo)/$range*175;
 $points=implode(' ',array_map(fn($i)=>$x($i).','.$y($chartValues[$i]),array_keys($chartValues)));
@endphp
<style>
.rbs{max-width:1450px;margin:0 auto;padding:4px 0 24px;color:#134e4a;font-size:13px}.rbs *{box-sizing:border-box}.rbs .panel{background:#fff;border:1px solid #ccfbf1;border-radius:18px;padding:23px}.rbs .row{display:flex;align-items:center;justify-content:space-between;gap:16px;flex-wrap:wrap}.rbs .eyebrow{font-size:10px;letter-spacing:1.3px;font-weight:750;color:#0f766e;text-transform:uppercase;background:#f0fdfa;padding:5px 8px;border-radius:6px;display:inline-block}.rbs h1{font-size:29px;font-weight:750;line-height:1.2;letter-spacing:-.7px;margin:12px 0 8px}.rbs h2{font-size:17px;font-weight:700;letter-spacing:-.3px}.rbs .muted{font-size:12px;color:#526f6b;line-height:1.7}.rbs .status{font-size:11px;padding:5px 9px;border-radius:15px;background:#f0fdfa;color:#526f6b;display:inline-block}.rbs .hero{background:linear-gradient(115deg,#fff 45%,#f0fdfa);margin-bottom:16px}.rbs .button{display:inline-flex;align-items:center;justify-content:center;gap:7px;border-radius:10px;padding:11px 16px;border:1px solid #ccfbf1;color:#0f766e;font-weight:650;font-size:12px;text-decoration:none;background:white;cursor:pointer}.rbs .primary{background:linear-gradient(120deg,#0d9488,#0F766E);border-color:transparent;color:white;box-shadow:0 5px 12px #0d948819}.rbs .share{background:linear-gradient(105deg,#f0fdfa,#fff);display:grid;grid-template-columns:minmax(0,1fr) 160px;gap:20px;margin-bottom:16px;overflow:hidden}.rbs .share h2{font-size:22px;margin:4px 0 6px}.rbs .share-row{display:flex;gap:9px;margin:16px 0 7px;flex-wrap:wrap}.rbs .share input{background:white;border:1px solid #ccfbf1;border-radius:10px;padding:12px;flex:1;min-width:160px;color:#526f6b;font-size:12px}.rbs .mascot{width:150px;height:160px;object-fit:contain;align-self:center}.rbs .metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:13px;margin-bottom:16px}.rbs .metric{color:inherit;text-decoration:none;min-height:149px;transition:transform .15s}.rbs .metric:hover{transform:translateY(-2px)}.rbs .metric:first-child{background:linear-gradient(135deg,#0d9488,#0C3D38);color:#fff;border-color:transparent}.rbs .metric:first-child .muted{color:#ccfbf1}.rbs .metric strong{font-size:29px;font-weight:750;letter-spacing:-.6px;display:block;margin:16px 0 4px}.rbs .metric-title{display:flex;gap:10px;align-items:center;font-size:12px}.rbs .icon{width:35px;height:35px;background:#f0fdfa;border-radius:12px;color:#0f766e;display:inline-flex;align-items:center;justify-content:center;flex-shrink:0}.rbs .metric:first-child .icon{background:#ffffff25;color:white}.rbs .main-grid{display:grid;grid-template-columns:minmax(0,2fr) minmax(290px,1fr);gap:16px}.rbs .stack{display:grid;gap:16px;align-content:start}.rbs .chart{width:100%;height:auto;display:block;margin-top:8px}.rbs .range{display:flex;gap:4px}.rbs .range a{padding:7px 10px;border-radius:8px;text-decoration:none;color:#526f6b;font-size:11px}.rbs .range a.active{background:#0d9488;color:white}.rbs .line-item{padding:14px 0;border-bottom:1px solid #dceee9;display:flex;gap:12px;align-items:center;text-decoration:none;color:inherit}.rbs .line-item:last-child{border-bottom:0}.rbs .line-item .grow{flex:1}.rbs .note{padding:12px 14px;background:#f0fdfa;border-radius:10px;color:#526f6b;font-size:11px;line-height:1.7;margin-top:14px}.rbs table{width:100%;border-collapse:collapse;font-size:12px}.rbs th{font-weight:500;color:#526f6b;background:#f0fdfa;padding:11px;text-align:left}.rbs td{border-bottom:1px solid #dceee9;padding:13px 11px}.rbs .empty{text-align:center;padding:30px 15px;color:#526f6b}.rbs .empty img{width:62px;height:62px;object-fit:contain;margin:0 auto 12px}.rbs .terms{padding:14px 0;border-bottom:1px solid #dceee9}.rbs .terms strong{font-size:19px;color:#0f766e}.rbs select{border:1px solid #ccfbf1;background:white;border-radius:8px;padding:8px;color:#526f6b;font-size:12px}.rbs a:focus-visible,.rbs button:focus-visible,.rbs input:focus-visible{outline:3px solid #5eead4;outline-offset:3px}.rbs .link{color:#0f766e;text-decoration:none;font-weight:600;font-size:12px}@media(max-width:1100px){.rbs .metrics{grid-template-columns:repeat(2,1fr)}.rbs .main-grid{grid-template-columns:1fr}.rbs .stack.side{grid-template-columns:1fr 1fr}}@media(max-width:600px){.rbs .panel{padding:17px}.rbs h1{font-size:24px}.rbs .share{grid-template-columns:1fr}.rbs .mascot{display:none}.rbs .stack.side{grid-template-columns:1fr}.rbs .metric strong{font-size:23px}.rbs .metric{min-height:135px}.rbs .share-row .button{width:100%}}
</style>
<div class="rbs">
 <header class="panel hero"><div class="row"><div><span class="eyebrow">Your subscription referral dashboard</span><h1>{{ $program->name }}</h1><p class="muted">Share something you believe in. Follow the rewards you earn.</p></div><div><span class="status">Program: {{ ucfirst($program->status) }}</span> <span class="status">Membership: {{ ucfirst($membership->status) }}</span></div></div>
 <div class="row" style="margin-top:17px"><p class="muted">@if($d['campaign'])Campaign: {{ $d['campaign']['starts_on'] }} – {{ $d['campaign']['ends_on'] }} · {{ $d['campaign']['state'] }}@else Subscription Referral Program @endif · Your activity only</p><a class="link" href="{{ $detailsUrl }}">Program details & terms →</a></div></header>
 <section class="panel share" x-data="{ copyStatus: '' }"><div><span class="eyebrow">Your personal referral link</span><h2>A simple share. A new opportunity.</h2><p class="muted">Send your link to someone who could benefit from {{ $program->name }}.</p>
 @if($referralLink)<div class="share-row"><input id="referral-link" aria-label="Your referral link" x-ref="referralLink" type="text" readonly value="{{ $referralLink }}" @click="$el.select()"><button class="button primary" type="button" @click="(async () => { try { await navigator.clipboard.writeText($refs.referralLink.value); copyStatus='Link copied!'; } catch(e) { $refs.referralLink.focus(); $refs.referralLink.select(); copyStatus='Select and copy the link above.'; } })()">Copy referral link</button><a class="button" href="{{ $referralLink }}?preview=1" target="_blank" rel="noopener">Preview link ↗</a></div><p role="status" aria-live="polite" class="muted" x-text="copyStatus"></p><p class="muted">Your link identifies you in this program. Rewards depend on qualifying payments and the program’s rules.</p>
 @else<div class="note">Your referral link becomes available when the program and your membership are active and a website is configured.</div>@endif
 <div class="note" aria-label="Referral link click statistics"><div class="row" style="justify-content:flex-start;gap:24px"><div><strong style="font-size:22px">{{ number_format($d['linkClicks']) }}</strong><br>Total link clicks</div><div><strong style="font-size:22px">{{ number_format($d['uniqueBrowsers']) }}</strong><br>Unique browsers (estimated)</div><div><strong style="font-size:22px">{{ number_format($d['periodClicks']) }}</strong><br>Clicks in the last {{ $d['days'] }} days</div></div><p style="margin-top:10px">Short-link visits recorded since tracking was enabled. Repeat visits count as clicks. Unique browsers use a 30-day cookie, not verified people. Previews, known bots and signed-in visits by you or your company team are excluded. Earlier visits, direct website links and blocked tracking are not counted.</p></div>
 </div><img class="mascot" src="{{ asset('images/mascots/r-bunny-thumbs-up.webp') }}" alt="Referral Bunny cheering you on"></section>
 <div class="row" style="margin:4px 0 13px"><h2>Your results <span class="muted" style="font-weight:400">· All time</span></h2><form method="GET" class="row" style="gap:8px"><input type="hidden" name="program_id" value="{{ $program->id }}"><input type="hidden" name="days" value="{{ $d['days'] }}"><label for="reward-currency" class="muted">Reward currency</label><select id="reward-currency" name="currency">@foreach($d['currencies'] as $currency)<option @selected($currency===$d['currency'])>{{ $currency }}</option>@endforeach</select><button class="button" style="padding:8px 12px">Apply</button></form></div>
 <section class="metrics">
 @foreach([
 ['gift','Net recorded rewards',$money($d['net']),'After refund reversals · not a payout total',$rewardUrl],
 ['users','Paying customers',number_format($d['customers']),'Customers with a recorded '.$d['currency'].' payment',route('reseller.deals',$args)],
 ['renew','Recorded payments',number_format($d['payments']),'First payments and renewals · '.$d['currency'],$activityUrl],
 ['wallet','Ready for review',number_format($d['ready']),'Rewards past their hold · '.$d['currency'],$rewardUrl],
 ] as [$icon,$label,$value,$note,$url])<a class="panel metric" href="{{ $url }}"><div class="metric-title"><span class="icon">@include('tenant.programs._dashboard-icon',['icon'=>$icon])</span>{{ $label }}</div><strong>{{ $value }}</strong><p class="muted">{{ $note }}</p></a>@endforeach
 </section>
 <div class="main-grid"><div class="stack">
  @include('reseller.programs._click-chart')
  <section class="panel"><div class="row"><div><h2>Your reward activity</h2><p class="muted">Daily recorded rewards · {{ $d['currency'] }} · {{ $d['tz'] }}</p></div><nav class="range" aria-label="Reward chart range">@foreach([7,30,90] as $days)<a class="{{ $d['days']===$days?'active':'' }}" @if($d['days']===$days) aria-current="page" @endif href="{{ route('reseller.dashboard',$args+['days'=>$days,'currency'=>$d['currency']]) }}">{{ $days }}D</a>@endforeach</nav></div>
  <svg class="chart" viewBox="0 0 810 260" role="img" aria-label="Your daily recorded rewards for the past {{ $d['days'] }} days"><defs><linearGradient id="referrer-reward-fill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stop-color="#0d9488" stop-opacity=".2"/><stop offset="100%" stop-color="#0d9488" stop-opacity=".02"/></linearGradient></defs>
  @for($i=0;$i<5;$i++)@php $tick=$lo+$range*$i/4; @endphp<line x1="60" x2="780" y1="{{ $y($tick) }}" y2="{{ $y($tick) }}" stroke="#dceee9"/><text x="50" y="{{ $y($tick)+4 }}" text-anchor="end" fill="#526f6b" font-size="10">{{ number_format($tick,2) }}</text>@endfor
  <polygon points="60,{{ $y(0) }} {{ $points }} 780,{{ $y(0) }}" fill="url(#referrer-reward-fill)"/><polyline points="{{ $points }}" fill="none" stroke="#0f766e" stroke-width="2.5"/>
  @foreach($chartValues as $i=>$value)<circle cx="{{ $x($i) }}" cy="{{ $y($value) }}" r="2.5" fill="#0f766e"><title>{{ $chartDates[$i] }}: {{ $d['currency'] }} {{ number_format($value,2) }}</title></circle>@endforeach
  @foreach([0,(int)floor((count($chartDates)-1)/2),count($chartDates)-1] as $i)<text x="{{ $x($i) }}" y="245" text-anchor="{{ $i===0?'start':($i===count($chartDates)-1?'end':'middle') }}" fill="#526f6b" font-size="10">{{ \Carbon\Carbon::parse($chartDates[$i])->format('M j') }}</text>@endforeach</svg>
  <p class="muted">{{ $d['start']->format('M j') }} – {{ $d['end']->format('M j, Y') }} · {{ $money(array_sum($chartValues)*100) }} net rewards in this period. Refund reversals may appear below zero.</p>
  <details style="margin-top:12px"><summary class="link" style="cursor:pointer">View daily amounts</summary><div style="max-height:180px;overflow:auto"><table><thead><tr><th>Date</th><th>Recorded reward</th></tr></thead><tbody>@foreach($d['daily'] as $date=>$amount)<tr><td>{{ $date }}</td><td>{{ $d['currency'] }} {{ number_format($amount,2) }}</td></tr>@endforeach</tbody></table></div></details>
  </section>
  <section class="panel"><div class="row"><div><h2>Recent activity</h2><p class="muted">Your latest {{ $d['currency'] }} payment and refund events</p></div><a class="link" href="{{ $activityUrl }}">View all activity →</a></div>
  @if($d['recent']->isEmpty())<div class="empty"><img src="{{ asset('images/logos/referralbunny-icon-only-128x128.webp') }}" alt=""><h2>Your first reward starts with a share</h2><p class="muted">No referred payments have been recorded for you in this program yet.</p></div>@else<div style="overflow:auto;margin-top:15px"><table><thead><tr><th>Activity</th><th>Reward</th><th>Date</th></tr></thead><tbody>@foreach($d['recent'] as $event)<tr><td>{{ $event->type==='refund'?'Refund reversal':'Subscription payment' }}<br><span class="muted">{{ ucfirst(str_replace('_',' ',$event->status)) }}</span></td><td>{{ $money($event->reward_minor) }}</td><td>{{ \Carbon\Carbon::parse($event->occurred_at,'UTC')->setTimezone($d['tz'])->format('M j, Y') }}</td></tr>@endforeach</tbody></table></div>@endif
  </section>
 </div><aside class="stack side">
  <section class="panel"><h2>Your reward status</h2><a class="line-item" href="{{ $rewardUrl }}"><span class="icon">◷</span><div class="grow"><strong>{{ $d['hold'] }} on hold</strong><p class="muted">Waiting for the hold period to end</p></div><span>›</span></a><a class="line-item" href="{{ $rewardUrl }}"><span class="icon">@include('tenant.programs._dashboard-icon',['icon'=>'gift'])</span><div class="grow"><strong>{{ $d['ready'] }} ready for company review</strong><p class="muted">Review does not mean paid</p></div><span>›</span></a><a class="line-item" href="{{ $messageUrl }}"><span class="icon">@include('tenant.programs._dashboard-icon',['icon'=>'users'])</span><div class="grow"><strong>{{ $d['unread'] }} unread messages</strong><p class="muted">Talk with this program’s admins</p></div><span>›</span></a><div class="note">Reward counts above use {{ $d['currency'] }}. Payment arrangements are handled by the company. Recorded rewards are not confirmed payouts.</div></section>
  <section class="panel"><div class="row"><h2>How you earn</h2><a href="{{ $detailsUrl }}" class="link">Full terms →</a></div>
  @forelse($validOffers as $offer)@php $v=$offer->currentVersion;$rules=$v->reward_rules ?? []; @endphp<div class="terms"><strong>{{ $v->reward_model==='percentage'?(float)$v->percentage_rate.'%':($v->reward_model==='fixed'?$v->currency.' '.number_format($v->fixed_amount,2):'Custom') }} reward</strong><p class="muted">{{ $offer->name }}</p><p style="margin-top:10px;font-size:12px">{{ ($rules['scope'] ?? '')==='recurring'?'Eligible payments for '.($rules['duration_months'] ?? 'the configured number of').' months from the first payment.':(($rules['scope'] ?? '')==='first_payment'?'First eligible payment only.':'See the published offer for reward eligibility.') }}</p><p class="muted">@if(isset($rules['hold_days'])){{ $rules['hold_days'] }}-day hold · @endif{{ ucfirst(str_replace('_',' ',$v->qualifying_event ?? 'See program terms')) }}</p></div>@empty<p class="muted" style="margin-top:12px">No active reward offer published yet.</p>@endforelse
  <p class="muted" style="margin-top:14px">Historical rewards keep the offer version applied when the event was recorded.</p><a class="button" style="width:100%;margin-top:15px" href="{{ $messageUrl }}">Message company admins →</a></section>
 </aside></div>
</div>
