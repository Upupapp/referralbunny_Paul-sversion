# Referral Bunny memory

## Hard rule — deployment and CI costs

Recorded: 2026-09-20. Source: explicit user instruction.

No GitHub Actions credits or runner minutes may be consumed as a consequence of a push or pull request. This is a hard rule, not an optimization preference. Do not rely on free allowances or skip-CI commit messages as the only safeguard.

Enforcement:
- GitHub Actions is disabled for `Upupapp/referralbunny_Paul-sversion` at repository level.
- Existing workflow triggers are inert while repository Actions are disabled. The current GitHub credential lacks workflow-edit scope; do not request broader access just to change those triggers. Never re-enable Actions or manually run workflows without an explicit user override.
- Verify repository Actions remain disabled before each push. Test locally and use direct SSH deployment to the existing server.
- Avoid Netlify deployment credits; reuse dependencies and batch changes into one asset build per application release.

The previous application release was deployed before this hard rule was given. Prior Actions consumption cannot be undone. Do not repeat it.

## Communication preference

Recorded: 2026-09-20. Always finish task updates with five recommended next steps. When the user must decide, state the recommended choice and explain why.

## Hard rule — DO NOT TOUCH LGUIDS

Recorded 2026-09-20 from explicit user instruction. LGU IDS (`lgu-ids`) must remain unchanged: screens, data, configuration, pricing, commissions, workflows, imports and integrations. Changes requested for the user’s Referral Bunny company account must be tenant-scoped; shared changes must preserve LGU IDS behavior and rendering. Never apply company-specific changes to LGU IDS.

## Program modes and navigation — requested 2026-09-20

Automated programs connected to online purchases should use the program-based dashboard, metrics, deals and pipeline as a system-wide standard, rather than an account-specific exception. Manual programs must use admin-configured rules and stages. LGU IDS is reference-only and remains untouched. This broader operating-mode rollout is pending; the current ProgramFinancialSummary still targets test-sp4s9i and must not be represented as a completed global rollout.

Upper menu bar: Tasks (including the deal-detail New Task shortcut), Overview/Full View, and the Google Calendar quick-connect button are LGU IDS-only. This is a navigation change, not removal of underlying task/calendar features.

Programs navigation: non-LGUIDS tenants have a main Programs tab immediately before Admin and an authorized, tenant-scoped topbar program picker with Add Program. The picker opens program workspaces; it does not filter aggregate dashboard data. New Deal is hidden on dashboard/deals aggregate pages when any visible program has an online connection record (including pending/disconnected setups); manual-only tenants keep it. Mixed-mode aggregate entry is withheld until explicit program assignment is implemented. LGUIDS retains its original controls.

Explicit operating modes added 2026-09-20: Programs now offer Manual/Automated during creation and in workspace Settings. New website quick-setup programs persist Automated. Existing null modes resolve from their existing connection record without a data backfill; LGUIDS always retains manual behavior without program queries. Mode changes are permitted only before launch on a draft with no integration record. Automated mode hides New Deal even before connection setup. This setting does not provision a connection, change reward calculations, or complete the pending system-wide metrics rollout.

Program workspace Analytics now uses program-scoped mode-aware metrics: automated programs read their own payment/refund ledger and current offer terms; manual programs show assigned referral counts and recorded stages without estimating commissions using the legacy fixed split. Workspace tab links load server data on navigation. The main tenant dashboard/deals-wide rollout remains pending; do not describe this workspace change as a completed global portal filter.

Main dashboard program selection added 2026-09-20: authorized company admins with a visible program receive a program-only dashboard using ProgramPerformanceSummary, avoiding tenant-wide legacy widgets/API aggregates. The dashboard picker uses a validated tenant-scoped program_id and remembers selection per company in the session; opening a program workspace also remembers it. Archived/deleted saved selections fall back to a visible default/first program. Cross-tenant explicit selections return 404. LGUIDS and legacy/no-program dashboards retain their original path. Deals/Pipeline pages and manual creation assignment are still pending.

Referrer portal 2026-09-20: non-LGUIDS navigation is Dashboard, Referral Programs, My Referrals, Rewards, Messages, Activity, Profile; My Programs still switches to company view. A tenant/referrer-scoped enrolled-program selector persists across dashboard/referrals/rewards/activity/messages. Automated views read only that referrer's attributed payment/refund events; manual views use program_id + reseller_id assignments, never name matching or a fixed reward split. Referrers without active/approved enrollment see an enrollment empty state. Non-LGUIDS referrer Request Forms routes return 404. LGUIDS keeps its legacy navigation/pages/messages unchanged.

Program conversations: new program_messages table supports referrer/company-admin messages per tenant+program+referrer, validated against enrollment and program-management permission, with separate unread states and paginated history. Company admins use Workspace > Messages; referrers choose Messages and a program. No test messages are sent in production. Existing legacy message tables are preserved, but new non-LGUIDS conversations use program-specific inboxes. Refresh loads new replies; no external email notifications are added by this feature.

Referrer share link 2026-09-20: selected automated program dashboards expose a readonly website URL and Copy referral link button for active program + active enrollment. Links use rb_program=program UUID and rb_ref=membership UUID, matching GetHired and snippet validators; no referral_code generation or data migration is needed. Destination comes from the same tenant/program connection, retains existing path/query/fragment, rejects credentials and non-HTTP(S) URLs. Copy failure selects the input for manual copying. LGUIDS path is unchanged. Link availability does not certify payment integration readiness.

## Referral program taxonomy — 2026-09-20

The user names the existing automated connected-platform setup **Subscription Referral Program**, for subscription business models. Current manual setup is **Manual Referral Program**, with LGUIDS serving only as reference and never changed. Public labels use these names; stored operating_mode values remain automated/manual to preserve integrations and historical rules. This rename does not force recurring rewards or change the admin's first-payment/recurring mechanics, company program names, or calculations.

**Marketplace Referral Program is future work only**: one program can contain many products, each with its own copyable referral link. Do not expose a selectable marketplace setup or claim product-level tracking is implemented yet. Future implementation must distinguish subscription vs marketplace from the underlying automated recording mode, with product catalog, product link attribution, and per-product reward rules designed separately.

Subscription dashboard redesign 2026-09-20: reference-inspired purple six-card layout, attention panel, top referrers, referral journey, official rabbit mascot. Scoped to automated selected-program dashboards only; manual and LGUIDS paths preserved. Date/currency filters apply to ledger money and ranking; new paying referral counts are first recorded payment per customer across all currencies, excluding renewals. MRR, active subscriptions, visitor/signups explicitly unavailable until lifecycle instrumentation exists. Persisted per-program referral target (total + inclusive start/end) plots total/day as a dashed line over overlapping filtered dates. Future actual graph points are omitted. Targets change planning only, not rewards. Separate program_referral_targets migration, no legacy tenant data updates.

Campaign duration integration 2026-09-20: Subscription dashboard Set/Edit duration writes existing referral_period_opens_at/referral_period_closes_at as UTC boundaries of inclusive program-local calendar dates. Full campaign is now the default reporting range when both dates exist; manual date filters override the view only. Existing target totals are preserved and their displayed period/daily average derives from the current campaign dates, including after Settings edits. Target writes cannot override a configured campaign period. Upcoming/In progress/Completed labels describe the calendar period, not program launch/pause status. No reward terms or lifecycle enforcement changed; duration does not automatically pause a program. Dashboard duration/target routes reject protected tenants and manual programs; LGUIDS remains unchanged.

Dashboard stitching 2026-09-20: money cards open same-program payment/reward records preserving date/currency; paying-customer card opens daily counts; unavailable subscription/MRR metrics lead to connection setup. Ready/on-hold attention cards have current all-date/all-currency queues matching the overview signals. Referrer names drill into their scoped records. Record views are read-only, not payout approval. Dashboard checks GetHired account/signup/payment readiness separately and links to connection details. Campaign duration form prefills existing target dates when campaign dates are absent. Chrome QA confirmed employer portal access and signup-ready connection, but payment tracking is not enabled and GetHired Billing History returned invoice-loading error; do not claim end-to-end paid conversion success.

Subscription referrer dashboard redesign 2026-09-20: automated Dashboard only uses the established green referrer layout with personal referral-link hero, official rabbit mascot, currency-specific lifetime rewards/paying customers/payment counts/review-ready metrics, 7/30/90-day personal reward chart, recent personal events, hold/review/message counts, campaign duration, and published offer mechanics. Manual programs, LGUIDS, and other existing referrer pages retain their previous rendering. No company revenue or other referrers' events are exposed. Refunds reduce net rewards; fully reversed payments are excluded from hold/review counts. Chart includes negative refund days and has an accessible daily table. No visitor, MRR, or paid-out values are fabricated.

## Portal color identity — 2026-09-20

Referrer portals use the established green/teal palette: sidebar #0C3D38, primary #0D9488, hover #0F766E, page #F0FDFA. Company admin portals remain purple. Apply this separation to new referrer views, including Subscription Referral Programs. Preserve the original rabbit brand assets. LGUIDS remains untouched.

Short referral links 2026-09-20: eligible automated program memberships receive a stable 8-character lowercase alias at /r/{code}, stored in program_referral_links separately from enrollment referral_code. The public route validates current same-tenant active program/membership and destination on every visit, then renders Open Graph/Twitter metadata and redirects browsers to the existing rb_program/rb_ref URL. Preview link (?preview=1) shows the destination without automatically following it. A public 1200×630 green rabbit banner is used, with program name and destination hostname; private descriptions, reward terms and referrer identity are not exposed. Unknown/paused/deleted/mismatched links return 404. Old long URLs still work. The original release did not record visits; click instrumentation was added below. No conversions are generated by the short-link endpoint; LGUIDS is excluded.

Short-link click analytics 2026-09-20: referrer subscription dashboard shows total recorded short-link visits, estimated unique browsers, and clicks in its selected 7/30/90-day window. Counters are scoped through the exact tenant/program/referrer memberships and are currency-independent. Public GET/HEAD/preview requests do not record a click: a short-lived encrypted page token is posted by browser JavaScript before automatic navigation, with CSRF protection, throttling and duplicate-event protection. Known crawler/prefetch requests and signed-in own-referrer/company-team visits are excluded. Unique browsers use a first-party HttpOnly 30-day cookie; only a per-membership HMAC is stored, never raw IP or user-agent. This is an estimate, not verified people; old clicks, old long URLs, JavaScript-disabled/blocked analytics and preview-continue visits are not included. Analytics errors cannot block navigation. Migration creates only a new empty analytics table; LGUIDS is excluded.

Daily referrer clicks chart 2026-09-20: green native SVG chart with 7/30/90-day shared dashboard filters, integer count axis, daily point tooltips, accessible daily-count table, period total, average across selected calendar days and peak daily count. Click timestamps group in the selected program timezone; totals and chart use the same scoped events and are currency-independent. Today is partial; pre-instrumentation dates mean no recorded data, not proven zero traffic. Manual programs and LGUIDS rendering remain unchanged.

Company subscription dashboard card update 2026-09-20: Referred MRR removed and replaced by Clicks immediately after Net referral revenue. Counts all recorded short-link visits for same-tenant/program memberships during the selected program-local dates; repeated visits count, currency does not filter clicks, and no historical click backfill is implied. Existing admin purple styling, remaining cards, manual and LGUIDS dashboards preserved.

Company Clicks drilldown 2026-09-20: dashboard card opens a permission-checked same-tenant/program report preserving date range and return currency. Report has date/referrer filters, total/average/peak, program-local daily counts, per-enrollment click/unique-browser/latest-click breakdown and 25-row paginated recent visits. Names are tenant-scoped and no visitor hashes/IPs are shown. Manual and protected tenants rejected. Counts include repeated visits and inactive members historical clicks, matching dashboard totals.

Admin click report chart 2026-09-20: purple daily line/area chart uses the report’s existing scoped daily totals and date/referrer filters, with 7/30/90-day shortcuts preserving referrer selection and return currency. Integer axes, point tooltips and existing accessible daily table. Future dates are not plotted; today is partial. No new tracking or data writes; LGUIDS untouched.

Verified GetHired signups 2026-09-20: short-link redirects carry an expiring encrypted click token through GetHired capture/claim; signed validation binds it to the enrolled membership. A confidential, owner-authorized GetHired feed exposes hashed customer IDs without names/emails. Referral Bunny imports idempotently every five minutes into new signup tables, validating same tenant/program and actual recorded click. Admin dashboard, click report and green referrer dashboard show verified signups by signup date, selected-period clicks leading to signup, and click-cohort conversion (later signups may count; historical unlinked signups never inflate conversion). Counts remain unavailable before first successful sync. Signups do not create rewards or imply payment. No LGUIDS changes; no production customer signup/payment is created for testing.

Program mechanics pages 2026-09-20: non-LGUIDS company program workspaces have a Mechanics tab; authorized referrer program details expose ?tab=mechanics, linked from subscription dashboards. Shared read-only content derives campaign dates, attribution window, active published same-program offers, reward rate/basis/scope, holds, approval and refund rules. GetHired explicitly explains saved signup attribution and the existing first-payment deadline from referral visit; no eligibility rule was changed. Manual programs use manual assignment wording and do not inherit online payment deadlines. No public anonymous access, payment-enablement claims or LGUIDS changes.
